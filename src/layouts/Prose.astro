<div
  class="prose dark:prose-invert
    prose-h1:font-bold prose-h1:text-3xl
  prose-a:text-blue-600 prose-a:no-underline prose-a:hover:underline dark:prose-a:text-blue-400 
    prose:break-words !max-w-none
    prose-img:rounded-xl prose-img:mx-auto prose-img:object-contain prose-img:max-h-[38rem]
  ">
  <slot />
</div>

<style is:global>
  @import "tailwindcss";
  @plugin "@tailwindcss/typography";
  @config "../../tailwind.config.mjs";

  figcaption {
    @apply text-center;
  }

  details {
    @apply my-4 p-2 border border-gray-300 dark:border-gray-600 
    bg-white dark:bg-slate-900 rounded-lg shadow-lg;
  }

  details summary {
    @apply hover:underline;
  }

  details summary::-webkit-details-marker {
    @apply inline;
  }
  .rlc-container {
    @apply w-full max-h-32 mt-0 mb-8 mx-auto text-black dark:text-white
    no-underline border border-gray-300 dark:border-gray-600 rounded-lg flex items-stretch
    shadow-lg overflow-hidden
    bg-white dark:bg-slate-900;

  /* width: 100%;
  max-width: 800px;
  max-height: 120px;
  margin: 0 auto 2rem; */
/* 
  color: black;
  text-decoration: none;

  border: 1px solid black;
  border-radius: 0.25rem;
  display: flex;
  align-items: stretch; */

  /* transition: background 200ms ease-in-out 0s, box-shadow 200ms ease-in-out 0s; */
}

.rlc-container:hover{
  /* background-color: rgba(80,80,80, 0.1);
  box-shadow: 0 4px 5px 2px rgba(80,80,80, 0.2); */
}

.rlc-info {
  @apply flex flex-col flex-[4_1_100px] min-w-0 overflow-hidden p-2 text-left justify-between ;
  /* overflow: hidden;
  padding: 0.5rem; */
  /* flex: 4 1 100px; */
  /* text-align: left;
  display: flex;
  flex-direction: column;
  justify-content: space-between; */
}

.rlc-title {
  @apply text-black dark:text-white text-lg font-bold line-clamp-1 text-ellipsis hover:underline;
  /* font-size: 1.25rem;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis; */
}

.rlc-description {
  @apply text-sm text-gray-700 dark:text-gray-300 overflow-hidden line-clamp-2 h-8 leading-4;
  /* color:#333; */
  /* color: #333;
  overflow: hidden;
  line-height:1rem;
  height: 2rem; */
}

.rlc-url-container {
  @apply flex flex-row items-center;
}

.rlc-favicon {
  /* margin-right: 4px; */
  /* width: 16px;
  height: 16px; */
  @apply m-0 mr-2 w-4 h-4;
}

.rlc-url {
  @apply text-base text-ellipsis whitespace-nowrap overflow-hidden line-clamp-1;
  /* font-size: 1rem;
  text-overflow: ellipsis;
  white-space: nowrap;
  overflow: hidden; */
}

.rlc-image-container {
  /* position: relative; */
  /* flex: 1 1 100px;z1 */
  @apply flex grow basis-[100px] min-w-0;
}

.rlc-image {
  /* object-fit: cover;
  width: 100%;
  height: 100%; */
  /* border-bottom-right-radius: 0.20rem;
  border-top-right-radius: 0.20rem; */
  @apply w-full h-full object-cover m-0 rounded-none;
}
</style>
